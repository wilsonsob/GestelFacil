<!-- Contexto: invoices_uniq_service -->
        <% @my_commissions.each do |commission| %>
          <% commission.contract.terms.each do |term| %>
            <% invoices_uniq_service = term.invoices.uniq{ |invoice| [invoice.number, invoice.service_code] }  %>

<!-- Check -->
                <tr>
                  <td style="padding-left:25px;">3) Os serviços localizados na fatura correspondem aos serviços contratados e vigentes?<p></p>
<!-- Contexto -->
<div class="" style="padding-left:25px;color:#007575;">
                     <% invoices_uniq_service.each do |invoice| %>
                      <strong><%= "#{invoice.service_code}"%></strong>
                      <%=" - #{invoice.service_name}" %>
                    <% end %>
                  </td>
                    <td class="text-center" style="padding-left:25px;">
                      <% if  %>
                        <i class="fas fa-check"></i>
                        <% else %>
                        <i class="fas fa-exclamation-triangle"></i>
                        <%#= invoice.date_invoice %>
                        <%#= "#{invoice.term.date_start} - #{invoice.term.date_end}"  %>
                        <% end %>
                  <% end %>
                  </div>
                  </td>
                </tr>
<!-- Contexto -->
    <% end %>
