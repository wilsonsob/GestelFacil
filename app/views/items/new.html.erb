<div class="container-md mt-4">
  <div class="row justify-content-center">
    <div class="col-8">
      <h6 style="text-align:center"><%= "Cadastrar novo item" %></h6>
      <h6 style="text-align:center"><%= "Contrato n. #{@term.contract.number}" %></h6>
      <h6 style="text-align:center"><%="#{@term.date_start.strftime("%d/%m/%Y")} - #{@term.date_end.strftime("%d/%m/%Y")}"%></h6>

        <div class="form m-3">
          <%= render 'shared/form_item' %>
        </div>
          <hr>

          <%= link_to "Voltar ao contrato n. #{@term.contract.number}", @term, class:"btn-custom-light" %>
          <%= link_to 'Meus contratos', commissions_path, class:"btn-custom-light" %>

    </div>
  </div>
</div>
