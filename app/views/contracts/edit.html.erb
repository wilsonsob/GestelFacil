<div class="container-md mt-5">
  <div class="row">
    <div class="col-8">
      <h5><strong><%= "Editar contrato #{@contract.number}" %></strong></h5>
        <div class="form m-5">
          <%= render 'shared/form', contract: @contract %>
        </div>
          <hr>

          <div class="btn btn-info mr-5">
            <%= link_to 'Detalhes do contrato', @contract %>
          </div>
          <div class="btn btn-light">
            <%= link_to 'Voltar', contracts_path %>
          </div>
    </div>
  </div>
</div>
