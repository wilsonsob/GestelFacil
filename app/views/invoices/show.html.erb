<div class="container-md mt-2">
  <meta charset="utf-8" />
    <div class="row justify-content-center">
      <div class="col-sm-10">
        <div class="card-body">

          <!-- Cabecalho -->
          <%= render 'header_report' %>

          <!-- Checklist -->
          <div class="card-body">
            <table class="table table-hover">
              <tr class="line-header">
                <th class="item-header" style="padding-left:25px;"><p></p><h6><strong>Itens conferência automática</strong></h6></th>
                <th class="result-reader text-center" style="padding-right:25px;"><p></p><h6><strong>Resultado</strong></h6></th>
              </tr>
              <!-- 1 -->
              <%= render 'cnpj' %>

              <!-- 2 -->
              <%= render 'competence' %>

              <!-- 3 -->
              <%= render 'services' %>

              <!-- 4 -->
              <%= render 'key_words' %>

              <!-- 5 -->
              <%= render 'fixed_price' %>

            </table>
          </div>

        </div>

          </table>

              <!-- Note -->
              <%= render 'form_note' %>

        </div>
        <br>
        
        <br>
      </div>
        <p style="text-align:center">
        <%= link_to 'Exportar relatório', invoices_path(format: :pdf), class:"btn-custom" %>
        </p>
  </div>
</div>
        <br><br>
        <%= @term %>
