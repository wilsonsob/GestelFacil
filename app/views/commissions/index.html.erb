<div class="container-md mt-5">
  <div class="row justify-content-center">
    <div class="col-sm-10">
      <h5 class="text-left"><strong><%= "Meus contratos" %></strong></h5><br>
        <table class="table table-hover" style="background-color:white; box-shadow:0 2px 4px #00000033; font-size:14px;">
          <tr>
          <th>Nº</th>
          <th>Objeto</th>
          <th>Contratada</th>
          </tr>
          <% @commissions.each do |commission| %>
            <tr>
            <td><%= link_to "#{commission.contract.number}", contract_path(commission.contract)  %></td>
            <td><%= commission.contract.object[0..70]  %>...</td>
            <td><%= commission.contract.company  %></td>
            </tr>
          <% end %>
        </table>
    </div>
  </div>
</div>
